{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Compile Java Project",
			"type": "shell",
			"command": "javac -d target/classes -cp target/classes:$(find ~/.m2/repository -name '*.jar' -path '*/javafx*' | paste -sd ':' -) $(find src/main/java -name '*.java' -type f)"
		}
	]
}